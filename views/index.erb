<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="UTF-8">
	<title>Counting stars</title>
</head>
<body>
<h1>Counting Stars</h1>
<% if !current_user.nil? %>
    <li>こんにちは<%= current_user.name %>さん</li>
    <li><a href="/signout">logout</a></li>
  <% end %>
<a href="/new"> CREATE NEW COUNT</a>

<% @count.each do |count| %>
	<img src="<%= count.img %>" width=300px><br>
	<a href ="/detail/<%= count.id %>"><%= count.title %></a><br>
	<%= count.number %> <br>
	<%= count.main_counter %><br>
	<form action="/count/<%= count.id %>" method="GET">
		<input type="submit" value="+">
	</form>

	<br>
<% end %>


</body>
</html>